# screenext

For now this only create an ipv6 UDP tunnel to the connected iOS device and start streaming your screen to it.
The iOS app to actually decode and display the stream data is WIP.

Video capturing currently only works on linux but after I get a first POC I'll add windows support
